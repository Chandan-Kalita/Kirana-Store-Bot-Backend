DATABASE_URL=
ANTHROPIC_API_KEY=
TELEGRAM_BOT_TOKEN=
PORT=
WEBHOOK_SECRET=
ANTHROPIC_BASE_URL=
GCP_PROJECT=
CLOUD_TASKS_QUEUE=
CLOUD_TASKS_LOCATION=
TASKS_INVOKER_SERVICE_ACCOUNT=
# TASK_HANDLER_PATH= (optional -- defaults to /tasks/handle, must match terraform's task_handler_path var)
# SERVICE_URL= (this service's own https URL, e.g. https://kirana-store-agent-xxx.a.run.app)